<section class="hero" aria-labelledby="hero-heading">
  <div class="container">
    <div class="content">
      <p class="eyebrow">Welcome to Acme Co.</p>
      <h1 id="hero-heading">Designing digital experiences that matter</h1>
      <p>
        We craft timeless brand identities and strategic digital products for forward-thinking
        companies ready to make their next bold move.
      </p>
      <div class="actions">
        <a class="button" href="/contact">Start a project</a>
        <a class="link" href="/about">Meet the team</a>
      </div>
    </div>
    <div class="card" role="presentation" aria-hidden="true">
      <div class="badge">15+ years</div>
      <p>Proven expertise in design, development, and brand storytelling.</p>
    </div>
  </div>
  <style>
    .hero {
      padding: 6rem 0 5rem;
    }

    .container {
      display: grid;
      gap: 3rem;
      align-items: center;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }

    .content h1 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      line-height: 1.1;
      margin: 0 0 1rem;
    }

    .content p {
      margin: 0 0 1.5rem;
      color: var(--color-muted);
      max-width: 36ch;
      font-size: 1.05rem;
    }

    .eyebrow {
      text-transform: uppercase;
      letter-spacing: 0.32em;
      font-size: 0.8rem;
      color: var(--color-accent);
      margin-bottom: 1rem;
      font-weight: 600;
    }

    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.9rem 1.75rem;
      border-radius: 999px;
      background: var(--color-accent);
      color: white;
      font-weight: 600;
      transition: transform 200ms ease, box-shadow 200ms ease;
      box-shadow: var(--shadow);
    }

    .button:hover,
    .button:focus {
      transform: translateY(-2px);
    }

    .link {
      font-weight: 600;
      color: var(--color-text);
    }

    .link:hover,
    .link:focus {
      color: var(--color-accent);
    }

    .card {
      background: var(--color-surface);
      border-radius: var(--radius);
      padding: 2rem;
      box-shadow: var(--shadow);
      position: relative;
      overflow: hidden;
    }

    .badge {
      display: inline-block;
      padding: 0.3rem 0.9rem;
      border-radius: 999px;
      background: rgba(47, 128, 237, 0.1);
      color: var(--color-accent);
      font-weight: 600;
      margin-bottom: 1rem;
    }
  </style>
</section>
